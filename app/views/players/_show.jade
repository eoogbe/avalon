section(data-bind='visible: $root.isCurrentPage("player")')
  h2 You are
    strong(data-bind='text: player().character')
  
  img.img-responsive(
    width='125'
    height='200'
    aria-hidden='true'
    data-bind='attr: { src: $root.player().characterImg(), alt: player().character }'
  )
  
  +knownPlayers('dl-horizontal wide-dl')
  
  button.btn.btn-default(type='button', data-bind='click: quest().update') Ready
